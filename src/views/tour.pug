extends base

block content
    include components/_tour-header
    +tour-header(tour.imageCover, tour.name, tour.duration, tour.startLocation.description)
    include components/_tour-main
    +tour-main()
    include components/_tour-pictures
    +tour-pictures()
    include components/_tour-map
    +tour-map()
    include components/_tour-opinions
    +tour-opinions()
